import routeMapper from '../../../utils/route-mapper';
import { module, test } from 'qunit';

module('Unit | Utility | route mapper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = routeMapper();
  assert.ok(result);
});
